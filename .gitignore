# 敏感信息
.env
config.json
config.json.bak*
config.example.json.bak*

# Python
__pycache__/
*.pyc
*.pyo
*.log

# 运行时数据（会话、音频输出、缓存）
data/
# 测试专用隔离实例数据目录（start_test_server.bat）
data-test/

# 临时分段目录与空静态目录
temp_seg/
static/

# 原子写中断残留保护
*.tmp

# 本地大文件（模型、CUDA 运行时、视频）
llm/bin/
llm/models/
大帅音频/

# 部署包（含密钥或为构建产物，随用随生成，不入库）
*.zip

# 临时调试脚本（仅根目录）
/scripts/
_preview.jpg

# 工具目录
.qoder/
.workbuddy/
_tmp_ui_check/

# 临时预览图（桌宠素材加工检查用）
_pet_preview/

# 本次桌宠画质优化临时脚本（v3 对比图等正式产物不入库位置）
.pet_tmp/

# 根目录临时调试产物（不入库）
/_cfg.json
/_openapi.json
/_role_news_payload.json
/_st.json
/_t.out
/_t2.out
/_tts_test.json

# ZCode 客户端工作目录（不入库）
/.zcode/

# APK 构建产物（从 android/ 工程重新构建即可，不入库）
release/

# Mimosa 安全扫描历史（不入库）
.mimosa/
