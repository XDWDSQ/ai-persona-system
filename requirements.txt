# 实测环境：Python 3.13（本机 venv 为 3.13.12；早期版本曾按 3.11.15 记录，属注记漂移）
# 依赖本身不挑 3.11/3.13，两者都可跑。
fastapi==0.141.1
uvicorn==0.52.1
httpx==0.28.1
pydantic==2.13.4
python-multipart==0.0.32
# HEIC/AVIF 图片转 JPEG（iPhone 照片默认 HEIC，云端视觉模型只认 bmp/gif/png/jpeg/webp）
pillow==12.3.0
pillow-heif==1.5.0

# 离线测试门（run_tests.bat / conftest.py 的 TestClient）——此前只在开发机全局装了，
# requirements 里没写，换机跑 run_tests.bat 直接 "No module named pytest"。
# TestClient 由 fastapi 自带（底层 httpx，已在上面）。
pytest==9.1.1
